---
bump: minor
---
Fleet control plane (M1 tracer): `cc-fleet hub` serves a profile, `cc-fleet join` enrolls a machine, and skills pushed from the hub land on every node within seconds. Full takeover of `~/.claude/skills` with an automatic backup before each apply, and `cc-fleet restore` to roll back locally.
